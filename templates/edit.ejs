
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>edit</title>
   <!--Bootstrap link-->
   <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-9ndCyUaIbzAi2FUVXJi0CjmCapSmO7SnpJef0486qhLnuZ2cdeRhO02iuK6FUUVM" crossorigin="anonymous">
   <!-- Bootstrap icons-->
   <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css">
   <!--static CSS file-->
   <link rel="stylesheet"  href="/styles/style.css">
   <!--Bootstrap javascript code-->
   <script defer src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js" integrity="sha384-geWF76RCwLtnZ8qwWowPQNguL3RmwHVBC9FhGdlKrxdiJJigb/j/68SIy3Te4Bkz" crossorigin="anonymous"></script>
</head>

<body class="d-flex justify-content-center align-items-center vh-100">
 
  <div class="container h-100">
    <div class="row d-flex justify-content-center align-items-center h-100">
      <div class="col-12 col-md-8 col-lg-6 col-xl-5">
        <div class="card" style="border-radius: 1rem;">
          <div class="card-body p-3 text-center">
            <div class="text-center">
              <h4 class="fw-bold mb-2 text-uppercase">Editar dados do pedido</h4>
            </div>
  
            <div class="md-5 mb-3 mt-4">
               
                <!--Filling data-->
              <form action="/edit/<%=body.id%>%>" method="POST">

                <div class="row text-start">
                  <div class="mb-4 col-6">
                    <label>Nome</label>
                    <input type="text" class="form-control" name="name" value="<%= body.name %>">
                  </div>

                  <div class="mb-4 col-6">
                    <label>Representante</label>
                    <input type="text" class="form-control" name="representative" value="<%= body.representative %>">
                  </div>
                               
                  <div class="mb-4 col-6">
                    <label >N° do pedido</label>
                    <input type="number" class="form-control" name="requestNumber" value="<%= body.requestNumber %>">
                  </div>

                  <div class="mb-4 col-6">
                    <label>Data</label>
                    <input type="date" class="form-control" name="date" value="<%=  body.date.toISOString().slice(0, 10) %>%>">
                  </div>
                     
                  <div class="mb-4 col-6">
                    <label>Envio</label>
                    <input type="text" class="form-control" name="shipping" placeholder="Opcional" value="<%= body.shipping %>">
                  </div>

                  <div class="mb-4 col-6">
                    <label>Descrição</label>
                    <textarea class="field form-control" name="description" placeholder="Opcional" rows="1"><%= body.description %></textarea>
                  </div>
                </div>

                <div class="d-grid">
                  <button class="btn btn-primary">Editar</button>
                  <a class="btn btn-dark mt-2" href="/">Cancelar</a>
                </div>

              </form>
  
            </div>
  
          </div>
        </div>
      </div>
    </div>
  </div>

</body>
</html>

